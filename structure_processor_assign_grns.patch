--- a/src/protos/processing/structure/structure_processor.py
+++ b/src/protos/processing/structure/structure_processor.py
@@ -1013,19 +1013,237 @@ class StructureProcessor(BaseProcessor):
         self.update_pdb_ids()
     
     # GRN methods
-    def assign_grns(self, grn_table, chain_ids=None, grn_chain_dict=None):
+    def assign_grns(self, protein_family='microbial_opsins', similarity_threshold=0.2, 
+                    use_mmseqs=True, save_results=True, reference_table=None):
         """
         Assign GRN annotations to structure data.
         
+        This method:
+        1. Extracts sequences from the loaded structures
+        2. Aligns them against a reference GRN table
+        3. Assigns GRN positions to each residue
+        4. Adds a 'grn' column to self.data
+        
         Args:
-            grn_table: DataFrame with GRN mappings
-            chain_ids: List of chain IDs to process
-            grn_chain_dict: Dictionary mapping chain IDs to GRN chain IDs
+            protein_family: Protein family for GRN assignment ('microbial_opsins', 'gpcr_a', etc.)
+            similarity_threshold: Minimum sequence identity for alignment (0.0-1.0)
+            use_mmseqs: Whether to use MMseqs2 (True) or BioPython (False) for alignment
+            save_results: Whether to save the GRN assignments
+            reference_table: Name of reference GRN table to use (defaults to family-specific)
+            
+        Returns:
+            Dictionary mapping chain IDs to their GRN assignments
         """
+        # Import required modules
+        from protos.processing.grn.grn_processor import GRNProcessor
+        from protos.processing.grn.grn_table_utils import init_row_from_alignment, expand_annotation
+        from protos.processing.grn.grn_utils import (
+            get_seq, sort_grns_str, GRNConfigManager, get_grn_interval
+        )
+        from protos.processing.sequence.seq_alignment import (
+            init_aligner, align_blosum62, mmseqs2_align2, format_alignment
+        )
+        import pandas as pd
+        
         if self.data is None:
-            raise ValueError("No data loaded")
+            raise ValueError("No structure data loaded")
+        
+        # Get sequences from structure data
+        self.logger.info("Extracting sequences from structure data...")
+        sequences = self.get_seq_dict()
+        
+        if not sequences:
+            self.logger.warning("No sequences found in structure data")
+            return {}
+        
+        # Initialize GRN processor
+        grn_proc = GRNProcessor(paths=self.paths)
+        
+        # Determine reference table based on protein family
+        if reference_table is None:
+            ref_table_map = {
+                'microbial_opsins': 'mo_ref',
+                'gpcr_a': 'gpcrdb_ref',
+                # Add more mappings as needed
+            }
+            reference_table = ref_table_map.get(protein_family, 'mo_ref')
+        
+        # Load reference GRN table
+        self.logger.info(f"Loading reference GRN table: {reference_table}")
+        ref_file = grn_proc.path_ref_dir / f"{reference_table}.csv"
+        
+        if not ref_file.exists():
+            raise FileNotFoundError(f"Reference GRN table not found: {ref_file}")
+        
+        # Load and clean the reference table
+        grn_proc.data = pd.read_csv(ref_file, index_col=0)
+        grn_proc.data = grn_proc.data.fillna('-')
+        grn_proc.ids = grn_proc.data.index.tolist()
+        
+        # Build reference sequences
+        ref_sequences = {}
+        for idx in grn_proc.ids:
+            row = grn_proc.data.loc[idx]
+            seq_parts = []
+            for val in row.values:
+                if val not in ['-', 'X', '.', ''] and not pd.isna(val):
+                    if isinstance(val, str) and len(val) > 0:
+                        seq_parts.append(val[0])
+            ref_sequences[idx] = ''.join(seq_parts)
+        
+        # Perform alignment
+        if use_mmseqs:
+            self.logger.info("Performing MMseqs2 alignment...")
+            alignment_df = mmseqs2_align2(sequences, ref_sequences)
+        else:
+            self.logger.info("Performing BioPython alignment...")
+            # Implement BioPython alignment for each sequence
+            alignment_results = []
+            aligner = init_aligner()
             
-        # Implementation would go here
-        self.logger.warning("GRN assignment not yet implemented in merged version")
+            for query_id, query_seq in sequences.items():
+                best_score = -float('inf')
+                best_match = None
+                
+                for ref_id, ref_seq in ref_sequences.items():
+                    alignment = align_blosum62(query_seq, ref_seq, aligner)
+                    score = alignment.score
+                    
+                    if score > best_score:
+                        best_score = score
+                        best_match = {
+                            'query_id': query_id,
+                            'target_id': ref_id,
+                            'sequence_identity': score / min(len(query_seq), len(ref_seq)),
+                            'e_value': 1.0,  # Placeholder
+                            'alignment': alignment
+                        }
+                
+                if best_match and best_match['sequence_identity'] >= similarity_threshold:
+                    alignment_results.append(best_match)
+            
+            alignment_df = pd.DataFrame(alignment_results)
+        
+        if alignment_df is None or alignment_df.empty:
+            self.logger.warning("No alignments found")
+            return {}
+        
+        # Filter by similarity threshold
+        alignment_df = alignment_df[alignment_df['sequence_identity'] >= similarity_threshold]
+        
+        # Get GRN configuration
+        config = GRNConfigManager(paths=self.paths)
+        grn_config_str = config.get_config(protein_family=protein_family, strict=True)
+        
+        if not grn_config_str:
+            # Use all GRN positions from reference
+            grns_str_str = list(grn_proc.data.columns)
+        else:
+            grns_str_str = []
+            for region_name, (start_grn, end_grn) in grn_config_str.items():
+                region_grns = get_grn_interval(start_grn, end_grn, grns_str=None)
+                grns_str_str.extend(region_grns)
+            grns_str_str = list(set(grns_str_str))
+        
+        grns_str_str = sort_grns_str(grns_str_str)
+        
+        # Process each chain
+        grn_assignments = {}
+        
+        for _, alignment in alignment_df.iterrows():
+            query_id = alignment['query_id']
+            ref_id = alignment['target_id']
+            
+            self.logger.info(f"Processing {query_id} (matched to {ref_id})")
+            
+            # Get sequences
+            query_seq = sequences[query_id]
+            ref_seq = ref_sequences[ref_id]
+            
+            # Align sequences
+            if use_mmseqs:
+                # Re-align for detailed mapping
+                aligner = init_aligner()
+                alignment_obj = align_blosum62(query_seq, ref_seq, aligner)
+            else:
+                alignment_obj = alignment['alignment']
+            
+            formatted = format_alignment(alignment_obj)
+            
+            # Create initial annotation
+            ref_row = grn_proc.data.loc[ref_id]
+            ref_dict = {grn: res for grn, res in ref_row.to_dict().items() if res != '-'}
+            seq_pos2grn = dict([(i + 1, grn) for i, grn in enumerate(list(ref_dict.keys()))])
+            
+            new_row = init_row_from_alignment(formatted, seq_pos2grn)
+            grns = [grn for grn in grns_str_str if grn in new_row.index]
+            new_row = new_row[grns]
+            
+            # Expand annotation
+            try:
+                grn_list, rn_list, missing = expand_annotation(
+                    new_row,
+                    query_seq.replace('-', ''),
+                    formatted,
+                    max_alignment_gap=1,
+                    protein_family=protein_family,
+                    verbose=0
+                )
+                
+                # Create GRN mapping for this chain
+                grn_mapping = dict(zip(rn_list, grn_list))
+                grn_assignments[query_id] = grn_mapping
+                
+                # Add GRN annotations to structure data
+                pdb_id, chain_id = query_id.rsplit('_', 1)
+                chain_mask = (self.data['pdb_id'] == pdb_id) & (self.data['auth_chain_id'] == chain_id)
+                
+                # Initialize GRN column if not exists
+                if 'grn' not in self.data.columns:
+                    self.data['grn'] = None
+                
+                # Assign GRN values
+                for idx in self.data[chain_mask].index:
+                    seq_id = self.data.loc[idx, 'auth_seq_id']
+                    res_key = f"{self.data.loc[idx, 'auth_comp_id']}{seq_id}"
+                    
+                    if res_key in grn_mapping:
+                        self.data.loc[idx, 'grn'] = grn_mapping[res_key]
+                
+            except Exception as e:
+                self.logger.warning(f"Failed to expand annotation for {query_id}: {e}")
+                # Use initial annotation if expansion fails
+                grn_assignments[query_id] = new_row.to_dict()
+        
+        # Save results if requested
+        if save_results and grn_assignments:
+            output_name = f"{self.name}_grn_assignments"
+            
+            # Convert assignments to DataFrame
+            grn_df = pd.DataFrame.from_dict(grn_assignments, orient='index')
+            if not grn_df.empty:
+                cols = grn_df.columns.tolist()
+                grn_df = grn_df[sort_grns_str(cols)].fillna('-')
+                
+                # Save using GRN processor
+                grn_proc.data = grn_df
+                grn_proc.dataset = output_name
+                grn_proc.ids = grn_df.index.tolist()
+                grn_proc.grns = grn_df.columns.tolist()
+                grn_proc.save_grn_table(dataset_id=output_name, normalize_formats=False)
+                
+                self.logger.info(f"Saved GRN assignments to {output_name}")
+        
+        return grn_assignments
     
     def get_grn_dict(self):
         """Get dictionary of GRN values."""